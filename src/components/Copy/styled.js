import { css } from '@emotion/core';

export const copyStyles = css`
  position: absolute;
  width: 100%;
  top: 20px;
  text-align: center;
`;

export const timeIsStyles = css`
  font-size: 1.7rem;
  font-weight: bold;
  letter-spacing: 0.05rem;
`;

export const runningStyles = css`
  font-size: 1.2rem;
  letter-spacing: 0.1rem;
`;

export const outStyles = css`
  font-size: 3rem;
  font-weight: bold;
  letter-spacing: 0.1rem;
`;